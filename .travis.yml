sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t pavelc/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true pavelc/docker-react npm run test
